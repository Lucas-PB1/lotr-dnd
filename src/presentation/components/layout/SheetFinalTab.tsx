import { SheetStitchView } from '../sheet/stitch/SheetStitchView';

/** Aba Ficha — modo de jogo (visualização + PV, combate e rolagens). */
export function SheetFinalTab() {
  return <SheetStitchView />;
}
